{
  "services": [
    {
      "type": "web",
      "name": "j-afrix-ai-api",
      "repo": "https://github.com/jadenafrix/j-afrix-ai-api",
      "env": "python",
      "plan": "starter",
      "buildCommand": "pip install -r requirements.txt",
      "startCommand": "uvicorn main:app --host 0.0.0.0 --port 10000",
      "autoDeploy": true
    }
  ]
}