services:
  - type: web
    name: cybix-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python index.py
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false
      - key: OWNER_NAME
        sync: false